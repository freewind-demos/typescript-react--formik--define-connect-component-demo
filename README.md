TypeScript React Formik Define Connect Component Demo
======================================================

如何使用formik的`connect`来定义一个ConnectedComponent

```
npm install
npm run demo
```

It will open page on browser automatically.
